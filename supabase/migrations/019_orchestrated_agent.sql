-- ============================================================
-- 019: тип агента «orchestrated» — маркер синтезу
--   Коли мозок (Claude) розбиває запит на під-задачі, виконує їх через
--   codex/gemini і зводить результати в одну відповідь, фінальний TaskResult
--   має agent_used:"orchestrated". Це НЕ маршрутизований виконавець (його
--   немає в каталозі agents / матриці доступів) — лише валідне значення для
--   стовпця tasks.agent, щоб UI показував джерело «Оркестратор».
-- ============================================================

alter type agent_type add value if not exists 'orchestrated';
