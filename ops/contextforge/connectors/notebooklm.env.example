# NotebookLM connector runtime config (materialize to notebooklm.env).
# NO secrets here — auth is the persistent Chrome profile on the
# cf_notebooklm_profile volume, seeded once (see connectors/notebooklm/README.md).

NOTEBOOKLM_TRANSPORT=http
NOTEBOOKLM_HOST=0.0.0.0
NOTEBOOKLM_PORT=3000

HEADLESS=true
BROWSER_CHANNEL=chromium

# Read-only surface: minimal profile + explicit disabled mutation/auth/session tools.
NOTEBOOKLM_PROFILE=minimal
NOTEBOOKLM_DISABLED_TOOLS=add_source,add_notebook,update_notebook,remove_notebook,generate_audio,download_audio,cleanup_data,setup_auth,re_auth,select_notebook,close_session,reset_session
